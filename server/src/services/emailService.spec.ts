import { IContactUsForm, sendContactUs, validateContactUsForm } from './emailService'

describe('emailService', () => {
   describe('validateContactUsForm', () => {
        it('should return false with invalid input.', () => {

        })

        it('should return true with valid input', () => {
            
        })
   }) 

   describe('sendContactUs', () => {
        it('should return true with valid input', () => {
            
        })
   })
})